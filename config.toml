title = "Sinclair's Software Blog"
description = "_csinclair_'s adventures in software engineering."
generate_feed = true


# The URL the site will be built for
base_url = "https://blog.sinclair.software"

taxonomies = [
    {name = "languages", feed = true},
    {name = "tags", feed = true},
]

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true
highlight_theme = "OneHalfDark"

[extra]
author = "Conor Sinclair"
top_menu = [
    {url = "$BASE_URL", name = "🏠 Home"},
    {url = "$BASE_URL/articles", name = "📒 Articles"},
    {url = "$BASE_URL/videos", name = "🎥 Videos"},
    {url = "$BASE_URL/languages", name = "🦄 Languages"},
    {url = "$BASE_URL/tags", name = "🏷️ Tags"},
]

